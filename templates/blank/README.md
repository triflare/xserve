# Blank Template

A minimal starting point with a core extension module.

## Usage example

1. Run `npm run init` and choose **Blank** (key: `blank`).
2. Add blocks in `src/01-core.js`.
3. Run `npm run build`.
